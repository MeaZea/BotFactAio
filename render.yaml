services:
  - type: web
    name: botfactaio
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: BOT_TOKEN
        value: ${BOT_TOKEN}